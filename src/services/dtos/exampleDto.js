class ExampleDto {
  constructor({ id, userName, memo, createdAt, updatedAt }) {
    this.id = id;
    this.userName = userName;
    this.memo = memo;
    this.createdAt = createdAt;
    this.updatedAt = updatedAt;
  }
}

export default ExampleDto;
